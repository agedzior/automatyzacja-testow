<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >

<suite name="Suite1" verbose="1">
    <parameter name="browserType" value="firefox"/> <!-- chrome / firefox -->
    <parameter name="implicitlyWait" value="10"/> <!-- INT -->
    <parameter name="windowMaximize" value="true"/> <!-- true / false -->
    <test name="Regression1">
        <classes>
            <class name="project.tests.LoginTest"/>
            <class name="project.tests.AddTestTest"/>
            <class name="project.tests.AddProjectTest"/>
            <class name="project.tests.AddUserTest"/>
        </classes>
    </test>
</suite>